node_modules/
dist/
build/
coverage/
.next/
.turbo/

# No app writes log files — logs go to Axiom. This catches tool output only,
# like pnpm-debug.log.
*.log
.DS_Store

# generated by next build
next-env.d.ts
*.tsbuildinfo

.claude/settings.local.json

# Secrets come from Infisical and no app reads a .env. These stay as a net, so
# a file created by hand can never be committed by accident.
.env
.env.*
