{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "NestJS services.",
    "The decorator options are not a preference: Nest resolves constructor",
    "injection from emitted metadata, and without them injection fails at",
    "runtime rather than at compile time."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "declaration": false,
    "sourceMap": true,
    "removeComments": false
  }
}
